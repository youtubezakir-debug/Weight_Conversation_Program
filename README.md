# Weight_Conversation_Program
A Python program that converts weight between different units. The user first enters a weight value, then selects the desired unit (such as kg or lb). The program performs the conversion and displays the converted result. This project demonstrates user input, conditional statements, arithmetic operations, and output formatting in Python.
